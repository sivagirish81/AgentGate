# AgentGate
Agents are treated as untrusted identities, not trusted insiders. They request short-lived, policy-scoped access for each task, with every action mediated and audited. Extends zero-trust infrastructure access, bringing Teleport-style identity, least privilege, and auditability to AI agents.
