# agentgate-bot-role.yaml
kind: role
metadata:
  name: agentgate-bot
spec:
  allow:
    kubernetes_labels:
      "*": "*"
    kubernetes_groups:
      - system:masters
    rules:
      - resources: ["*"]
        verbs: ["*"]
  deny: {}
version: v7
